# ComfyUI 单节点运行变全局执行问题修复总结

## 问题描述

在 ComfyUI 新版前端（v1.x+）中，点击节点的**单节点运行按钮**（蓝色播放按钮 / 右键 Run / Execute to selected output nodes）时，本应只执行目标节点及其上游依赖，但实际行为等同于点击了全局 **Queue Prompt**，执行了整个 workflow 的所有节点。

## 影响范围

以下 4 个插件均存在此问题：

| 插件 | 文件 |
|------|------|
| ComfyUI-Danbooru-Gallery | `js/native-execution/__init__.js` |
| Comfyui-LG_GroupExecutor | `web/queue_utils.js` |
| kaytool | `web/quickaddsetget.js` |
| SegmentQueue | `js/segment_queue.js` |

## 根本原因

### ComfyUI 新版前端的架构变更

新版 ComfyUI（v1.x+）的 `api.queuePrompt` 函数签名从 2 个参数变为 3 个参数：

```typescript
// 旧版
async queuePrompt(number: number, prompt: { output, workflow }): Promise<PromptResponse>

// 新版
async queuePrompt(
  number: number,
  prompt: { output, workflow },
  options?: QueuePromptOptions  // ← 新增！包含 partialExecutionTargets
): Promise<PromptResponse>
```

单节点运行时，ComfyUI 前端通过 `options.partialExecutionTargets` 数组传递目标节点 ID，**不再修改 `prompt.output` 的内容**（`prompt.output` 仍然是全量 workflow）。后端收到这个参数后，只执行指定的目标节点及其上游依赖。

### 插件的问题

上述 4 个插件都在前端 hook 了 `api.queuePrompt` 或 `app.queuePrompt`，但函数签名仍停留在旧版的 2 个参数：

```javascript
// 错误的旧版签名
api.queuePrompt = async function(index, prompt) {
    // options 参数被完全丢弃！
    return originalApiQueuePrompt.apply(this, [index, prompt]);
};
```

当新版 ComfyUI 调用 `api.queuePrompt(index, prompt, options)` 时：
1. 插件的 hook 只接收前 2 个参数
2. `options`（含 `partialExecutionTargets`）被丢弃
3. 后端收到全量 prompt 但不知道要部分执行
4. **结果：执行了全部节点，等同于全局 Queue Prompt**

此外，部分插件还存在其他问题：
- **Danbooru**：在 `!window._queueNodeIds` 时，只要检测到 `GroupExecutorTrigger` 节点，就把 prompt 过滤成只剩 Manager+Trigger，单节点运行也被误拦截
- **LG**：`app.queuePrompt` hook 漏了 `return`，且 `api.queuePrompt` 被 `.bind(this)` 破坏了 `this` 指向

## 修复方案

### 通用修复原则

所有 hook `api.queuePrompt` 或 `app.queuePrompt` 的插件，必须：
1. **函数签名增加 `options` 参数**
2. **透传 `options` 给原始函数**
3. **检测 `options.partialExecutionTargets` 存在时直接透传，不做任何过滤**

### 1. ComfyUI-Danbooru-Gallery

**文件**：`js/native-execution/__init__.js`

#### 修改点

1. `api.queuePrompt` 函数签名从 `(index, prompt)` 改为 `(index, prompt, options)`
2. 函数开头增加单节点/部分执行检测
3. 所有内部调用 `api._originalQueuePrompt` 的地方透传 `options`

#### 修复代码

```javascript
api.queuePrompt = async function (index, prompt, options) {
    logger.info('[OptimizedExecutionSystem] api.queuePrompt called');

    // ✅ CRITICAL FIX: 检测 ComfyUI 原生单节点/部分执行
    // 新版 ComfyUI 通过 options.partialExecutionTargets 传递目标节点
    if (options?.partialExecutionTargets?.length > 0) {
        logger.info('[OptimizedExecutionSystem] 检测到部分执行（单节点运行），跳过组执行过滤');
        return api._originalQueuePrompt.apply(this, [index, prompt, options]);
    }

    // 以下原有逻辑完全不变...
    // ...

    // 所有 api._originalQueuePrompt 调用都要透传 options:
    // return api._originalQueuePrompt.apply(this, [index, prompt, options]);
};
```

### 2. Comfyui-LG_GroupExecutor

**文件**：`web/queue_utils.js`

#### 修改点

1. `app.queuePrompt` hook 补 `return`
2. `api.queuePrompt` 去掉 `.bind(this)`，用闭包 `const self = this` 替代
3. `api.queuePrompt` 函数签名从 `(index, prompt)` 改为 `(index, prompt, options)`
4. 透传 `options` 给原始函数

#### 修复代码

```javascript
initializeHooks() {
    const originalQueuePrompt = app.queuePrompt;
    const originalGraphToPrompt = app.graphToPrompt;
    const originalApiQueuePrompt = api.queuePrompt;

    // FIX 1: 必须 return 原始 Promise
    app.queuePrompt = async function() {
      this.processingQueue = true;
      this.eventManager.dispatchEvent("queue");
      try {
        return await originalQueuePrompt.apply(app, [...arguments]);
      } finally {
        this.processingQueue = false;
        this.eventManager.dispatchEvent("queue-end");
      }
    }.bind(this);

    app.graphToPrompt = async function() {
      this.eventManager.dispatchEvent("graph-to-prompt");
      let promise = originalGraphToPrompt.apply(app, [...arguments]);
      await promise;
      this.eventManager.dispatchEvent("graph-to-prompt-end");
      return promise;
    }.bind(this);

    // FIX 2: api.queuePrompt 不能 .bind(this)，要保持 this 指向 api 对象
    // 用 self 闭包来访问 QueueManager 实例
    const self = this;
    // FIX 3: 增加 options 参数并透传
    api.queuePrompt = async function(index, prompt, options) {
      if (self.queueNodeIds && self.queueNodeIds.length && prompt.output) {
        const oldOutput = prompt.output;
        let newOutput = {};
        for (const queueNodeId of self.queueNodeIds) {
          self.recursiveAddNodes(String(queueNodeId), oldOutput, newOutput);
        }
        prompt.output = newOutput;
      }
      self.eventManager.dispatchEvent("comfy-api-queue-prompt-before", {
        workflow: prompt.workflow,
        output: prompt.output,
      });
      // ✅ 透传 options，否则 partialExecutionTargets 会丢失
      const response = originalApiQueuePrompt.apply(api, [index, prompt, options]);
      self.eventManager.dispatchEvent("comfy-api-queue-prompt-end");
      return response;
    };

    // ... 其余代码不变
}
```

### 3. kaytool

**文件**：`web/quickaddsetget.js`

#### 修改点

1. `api.queuePrompt` 函数签名从 `(index, prompt)` 改为 `(index, prompt, options)`
2. 透传 `options` 给原始函数

#### 修复代码

```javascript
const originalApiQueuePrompt = api.queuePrompt;
// ✅ FIX: 增加 options 参数并透传，否则 partialExecutionTargets 丢失
api.queuePrompt = async function (index, prompt, options) {
    if (KayToolState.queueNodeIds?.length && prompt.output) {
        const oldOutput = prompt.output;
        let newOutput = {};
        for (const queueNodeId of KayToolState.queueNodeIds) {
            kayRecursiveAddNodes(String(queueNodeId), oldOutput, newOutput);
        }
        prompt.output = newOutput;
    }
    return originalApiQueuePrompt.apply(this, [index, prompt, options]);
};
```

### 4. SegmentQueue

**文件**：`js/segment_queue.js`

#### 修改点

1. `app.queuePrompt` 函数签名从 `(number, batchCount)` 改为 `(number, batchCount, options)`
2. 所有 `origQueuePrompt` 调用处透传 `options`

#### 修复代码

```javascript
app.queuePrompt = async function(number, batchCount, options) {
    const sqrNodes = (app.graph?.nodes || []).filter(n =>
        n.type === "SegmentQueueRunner" && !n.muted && n.mode !== 4
    );
    if (sqrNodes.length === 0) {
        return origQueuePrompt(number, batchCount, options);  // ✅ 透传
    }

    // ... 中间逻辑不变 ...

    let submitResult;
    try {
        // ... 精简提交逻辑 ...
        submitResult = await res.json();
    } catch (e) {
        console.warn("[SQR] 精简提交失败，回退到完整 prompt:", e);
        // ✅ FIX: 透传 options 参数
        submitResult = await origQueuePrompt(number, batchCount, options);
    }
    return submitResult;
};
```

## 修复验证

1. 覆盖修改后的文件到对应插件目录
2. 重启 ComfyUI 后端
3. 浏览器强制刷新（Ctrl+F5 或 Cmd+Shift+R）清除缓存
4. 测试单节点运行：
   - 右键点击节点 → Run
   - 点击节点右上角的蓝色播放按钮
   - 选中输出节点后按 Alt+R
5. 观察网络请求的 Payload：
   - 正常情况：`extra_data.extra_pnginfo.workflow` 中应包含 `partialExecutionTargets` 字段
   - 如果只执行了目标节点，说明修复成功

## 兼容性说明

- **新版 ComfyUI（v1.x+）**：修复后单节点运行正常工作
- **旧版 ComfyUI**：`options` 参数为 `undefined`，修复代码不会触发任何新逻辑，**与修复前行为完全一致**，无回归风险

## 技术背景

### 为什么新版 ComfyUI 不改前端而改后端？

旧版 ComfyUI 单节点运行时，前端会预先过滤 `prompt.output`，只保留目标节点及其上游依赖，然后发送过滤后的 prompt 到后端。这种方式的问题是：
- 前端过滤逻辑复杂，容易出错
- 不同插件对过滤规则的理解可能不一致

新版 ComfyUI 改为：前端发送**全量 workflow**，通过 `options.partialExecutionTargets` 告诉后端要执行哪些节点。后端根据这个信息，结合自己的缓存机制，只计算需要重算的节点。这种方式：
- 前端逻辑简化
- 后端可以更好地利用缓存
- 但需要所有 hook `queuePrompt` 的插件同步更新

### 为什么只有部分插件受影响？

只有那些**hook 了 `api.queuePrompt` 或 `app.queuePrompt`** 的插件才会受影响。如果插件只是注册节点、添加 UI、处理事件，没有拦截队列提交流程，则不会触发此问题。
